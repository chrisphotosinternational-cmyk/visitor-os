# Configurations

This folder contains example business configuration files.

These files are examples only. They must not include secrets, passwords, real private customer data, or API keys.

The core VISITOR-OS engine must stay generic. Business differences are represented through configuration.
