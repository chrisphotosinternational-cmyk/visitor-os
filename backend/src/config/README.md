# Config

High-level configuration module placeholder.

The foundation environment loader currently lives in `src/core/config` because it is core infrastructure.

Future non-environment configuration mapping can live here when needed.
